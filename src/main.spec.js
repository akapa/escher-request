'use strict';

const test = require('tape-promise/tape');
// const test = require('tape');
const escherRequest = require('./main');

test('making requests', async t => {
  // t.test('should find escher credentials based on url and set escher headers', async t => {
  //   t.equal(1, 3);
  //   t.end();
  // });

  t.equal(1, 1);
  // t.end();
});
